package agentlaunch

import "testing"

// TestPackagePresent is a placeholder so `go test ./...` exits 0 against
// the otherwise-empty agentlaunch package while sibling Phase 1 subagents
// fill in the LaunchPlan / CompiledLaunch / PreparedLaunch surface.
func TestPackagePresent(t *testing.T) {
	t.Log("agentlaunch package compiles")
}
